--// This is intended to be a module script under the 'Loader.lua' file

local module = {
Whitelist = {
		Enabled = false,
		Group = 000000, -- GroupID 
		IDs = {}, -- Group Ranks to apply to, put 'all' to do all.
		Users = {} -- Supports UserIDs and names/displaynames
}	
	
}

return module
